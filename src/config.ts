// ── Application configuration ────────────────────────────────────────────────

/** Base URL of the backend API (used in generated bash scripts). */
export const API_BASE = 'http://localhost:5064'

/** Auto-logout after this period of inactivity (ms). */
export const INACTIVITY_MS = 60 * 60 * 1000 // 1 hour

/** Number of log entries per page. */
export const LOGS_PAGE_SIZE = 50
