apiVersion: v1
kind: Service
metadata:
  name: lb-web
  namespace: default
spec:
  type: NodePort
  selector:
    app: lb-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30082
